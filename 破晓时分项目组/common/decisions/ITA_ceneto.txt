ITA_senet_categories = {
	senet_locked = { #元老院：锁定决议
		cost = 0 #花费（政治点）
        visible = { #什么情况可见
            country_exists = ITA
			always = yes	
        }
        available = { #满足什么条件能点
			always = no
        }
        fire_only_once = yes #只能选择一次（yes/no)
        days_remove = 3 #几天后移除
		select_effect = { #选择时的效果

		}
		modifier = { #持续时间内的效果

		}
        remove_effect = { #移除后的效果
			
        }
		ai_will_do = { #AI倾向
			factor = 0
		}
	}
	
}